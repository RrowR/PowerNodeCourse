server:
    port: 8888
spring:
    application:
        name: config-server
    cloud:
        config:
            server:
                git:
                    uri: https://gitee.com/rrow/globleconfig.git
                    username: Rrow
                    password: dong4297084
                    search-paths: /config
                    basedir: ../../../local-config      # 根项目路径的同级路径
