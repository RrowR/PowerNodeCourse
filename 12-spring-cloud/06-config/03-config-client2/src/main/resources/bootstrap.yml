server:
    port: 8081
spring:
    application:
        name: config-client2
    cloud:
      config:
          uri: http://localhost:8888
          profile: dev
          name: config
          label: master
    rabbitmq:
        host: rrow.xyz
        port: 5672
        username: admin
        password: 123456
        virtual-host: /

management:
    endpoints:
        web:
            exposure:
                include: '*'   # 暴露监听的端口使用
