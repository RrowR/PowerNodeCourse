server:
    port: 80
spring:
    application:
        name: gateway-server
    cloud:
        gateway:
            enabled: true
            routes:
                - id: search-service-router
                  uri: http://localhost:8080
                  predicates:
                      - Path=/gossip
#                - id: search-service-router
#                  uri: http://localhost:8080
#                  predicates:
#                      - Path=/doSearch