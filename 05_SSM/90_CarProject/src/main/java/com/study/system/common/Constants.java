package com.study.system.common;

/*
    配置常量类，用接口，因为接口里的方法都是final && static
 */
public interface Constants {
    /*
        全局公共对象user(在Application里)
     */
    String CURRENT_SESSION_USER_KEY = "user";
}
