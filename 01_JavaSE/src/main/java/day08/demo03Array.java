package day08;

/**
 * 栈和堆：
 *     栈: 用来存储局部变量
 *      特点：
 *       1.栈内存符合"先进后出，后进先出"的特点
 *       2.栈内存是一块连续的存储空间，由虚拟机分配，效率高
 *       3.虚拟机会为每一个线程创建一个栈，用于存放该线程执行方法的信息
 *       4.栈内存由虚拟机自动管理，程序员无需管理栈内存
 *     堆：用来存储new出来的对象和数组
 *      特点：
 *       1.不是一块连续的存储空间，分配灵活，但是效率低
 *       2.虚拟机中只有一个堆内存，被所有的线程共享
 *       3.堆内存本质上需要程序员手动来管理，但是实际上会被"垃圾回收"
 *
 *
 */
public class demo03Array {
    public static void main(String[] args) {

    }
}
